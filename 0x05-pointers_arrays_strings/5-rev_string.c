#include "main.h"

/**
 * rev_string - reverses integer
 * @s: parameter
 * Return: return integer
 */

void rev_string(char *s)
{
	int c = 0;

	while (s[c] != '\0')
	{
		c++
	}
	for (c = c - 1; c >= 0; c--)
	{
		*s = s[c];
	}
}
